export {}
// [idNum, username]

let user: [number, string]

user = [1, 'jcalaop']

console.log('user', user)

// Tuplas con varios valores

// [id, username, isPro]

let userInfo: [number, string, boolean]
userInfo = [2, 'calaojuanpablo', true]

console.log('userInfo', userInfo)

// Arreglo de tuplas
let array: [number, string][] = []

array.push([1, 'jcalaop'])
array.push([2, 'calaojuanpablo'])

console.log('Array of tuples', array)
